@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Yönetim Paneli</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/custom.css" rel="stylesheet" />
    <style>
        /* Sidebar styles */
        .sidebar {
            width: 250px;
            position: fixed;
            top: 0;
            left: 0;
            bottom: 0;
            background-color: #212529; /* Darker sidebar */
            padding-top: 20px;
            color: #fff;
            transition: width 0.3s;
        }

        .sidebar-header {
            padding: 15px;
            text-align: center;
            background-color: #1f262b; /* Slightly darker for header */
            font-size: 35px;
            font-weight: bold;
            color: #fca311;
        }

        .sidebar ul {
            padding-left: 0;
            list-style: none;
        }

            .sidebar ul li {
                padding: 15px 20px;
            }

                .sidebar ul li a {
                    text-decoration: none;
                    color: #adb5bd;
                    display: block;
                    font-size: 20px;
                    padding: 10px 20px;
                    border-radius: 4px;
                    transition: background-color 0.3s, padding-left 0.3s;
                }

                    .sidebar ul li a:hover {
                        background-color: #e03e3e;
                        color: #fff;
                        padding-left: 30px;
                    }

        /* Content area styles */
        .content {
            margin-left: 250px;
            padding: 20px;
            background-color: #181d23; /* Dark content background */
            color: #fff;
            transition: margin-left 0.3s;
        }

        header {
            background-color: #c82333; /* Darker red for header */
            color: #fff;
            padding: 20px 30px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }

            header h2 {
                margin: 0;
                font-size: 28px;
                font-weight: bold;
            }

        main {
            padding-top: 20px;
            font-family: Arial, sans-serif;
        }

        /* Button Styles for Larger Buttons */
        .btn {
            padding: 12px 24px;
            font-size: 18px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
        }

            .btn-primary:hover {
                background-color: #0056b3;
            }

        .btn-secondary {
            background-color: #6c757d;
            border: none;
        }

            .btn-secondary:hover {
                background-color: #5a6268;
            }

        /* Table styles */
        table {
            border-collapse: collapse;
            width: 100%;
        }

            table th,
            table td {
                text-align: left;
                padding: 12px;
                border-bottom: 1px solid #444;
                color: #dcdcdc; /* Lighter color for table text */
            }

            table th {
                background-color: #e67e22; /* Dark orange background */
                color: #d35400;
                font-weight: bold;
            }

            table tbody tr:hover {
                background-color: #343a40;
            }

        /* Modal styling */
        .modal-content {
            background-color: #495057;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="sidebar-header">
                <h4>Admin Paneli</h4>
            </div>
            <ul>
                <li><a href="/AdminBlog/Index">Bloglar</a></li>
                <li><a href="/AdminComment/Index">Yorumlar</a></li>
                <li><a href="/AdminHotel/Index">Oteller</a></li>
                <li><a href="/AdminRestaurant/Index">Restoranlar</a></li>
                <li><a href="/AdminContact/Index">Gelen Mesajlar</a></li>
                <li><a href="/AdminAbout/About">Hakkımda</a></li>
                <li><a href="/Default/Index">Siteye Git</a></li>
                <li><a href="/AdminLogin/Logout">Çıkış Yap</a></li>
            </ul>
        </nav>

        <!-- Main Content -->
        <div class="content flex-grow-1">
            <!-- Page Content -->
            <main>
                @RenderBody()
            </main>
        </div>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("Scripts", required: false)

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var commentModal = new bootstrap.Modal(document.getElementById('commentModal'));

            document.querySelectorAll('a[data-bs-toggle="modal"]').forEach(function (button) {
                button.addEventListener('click', function () {
                    var comment = button.getAttribute('data-context');
                    document.getElementById('modalCommentContent').textContent = comment;
                });
            });
        });
    </script>
</body>

</html>
